#include "foo.cc"
// #include <iostream>

typedef unsigned long int uli;
typedef uli vli;

  Foo::week day;
  Foo::A r;
  Foo::B q;

  lli m;


void fun(int arg_x) {
    // std::cout << (arg_x*arg_y-arg_x-arg_y+1) << std::endl;
      int u;
            Foo::A r;
      Foo::B q;
  }

int main() {
  int local_p = 1, local_q = 1;

  vli omg = 9;

  omg += 1;

  // q.a = 'r';
  
  // std::cin >> local_p >> local_q;
  Foo::fun(local_p, local_q);
  if( Foo::tfun<int, float>(3, 4) ) {
  	// std::cout << local_p * local_q << std::endl;
  }
  if( Foo::tfun<char, char>(3, 4) ) {
  	// std::cout << local_p * local_q << std::endl;
  }
  if( Foo::tfun<float, float>(3, 4) ) {
  	// std::cout << local_p * local_q << std::endl;
  }

}
