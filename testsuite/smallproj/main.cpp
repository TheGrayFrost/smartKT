#include <iostream>
#include "foo.hpp"

#define F f(
#define CALLF F v

unsigned long int p[][3] = {{1, 2, 3}, {4, 5, 6}};

int main()
{
	A::m = 31;
	A * u = CALLF \
	);
	A z[7];
	A * x = new A [5];
	for (int i = 0; i < 5; ++i)
	{
		z[i].r++;
		x[i].r++;
		x[i].r = 7;
	}
	delete [] x;
	std::cout << "u->r = " << u->r << "\n";
	return 0;
}
